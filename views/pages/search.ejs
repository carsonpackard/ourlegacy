<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head'); %>
</head>
<body class="container">
<style>
  ul {
    list-style-type: none;
}
.result{
  color: black;
  text-decoration: none;
  overflow:visible;
  white-space: nowrap;
  text-overflow: ellipsis;
  display: inline-block;
  
}
.result:hover{
  color: black;
  text-decoration: none;
}
.subtitle{
  color: black;
  text-decoration: none;
  margin-left: 20%;
  display: list-item;
}
.subtitle:hover{
  color: black;
  text-decoration: none;
}
.outer{
  width: 100%;
  margin: 0 auto;
}
div a {

  margin-bottom: auto;
}
.info{
  display: block;
  max-width:150px;
  max-height:100px;
  width: auto;
  height: auto;
  display:inline-block;
}

</style>
<header>
  <%- include('../partials/header'); %>
</header>

<main>
  <div class="jumbotron">
    <a href="/">Back</a>

    <h2>Results</h2>
    <div id="scroll" class="scroll" style="width:100%;height:100%;">
      
      <ul>

        <% for (var i = 0; i < results.length; i++) {%>
          
          <li style="margin-top: 5%">
            <a class="result" href="/media/<%= results[i].media._id%>" width="100%"> 
              <div>
                <object class="info" data="/images/<%= results[i].media.thumbnail%>" type="image/png" >
                  <img class="info" src="/images/default.jpg" >
                </object>
                <div class="result" style="margin-left: 2%;font-size: x-large;font-weight: bolder;color: rgb(56, 56, 255);" width="100%"> <%= results[i].media.title%></div>
              </div>
            </a>
          </li>
          

          <% for (var j = 0; j < results[i].subtitles.length; j++) {%>

              <a class="subtitle" href="/media/<%= results[i].media._id%>?t=<%= results[i].subtitles[j].start /1000%>"> 

                  <% if( (results[i].subtitles[j].start / 1000) < 3600){ %>
                    <%= new Date(results[i].subtitles[j].start).toISOString().substring(14, 19) %>
                    <% } else{ %>  
                    <%= new Date(results[i].subtitles[j].start).toISOString().substring(11, 16) %>
                 <% } %>  : <%=results[i].subtitles[j].text%>
                 <% } %>
                </a>
                 <!-- Nested loop below --> 
                 <% } %>

              
            </div>

        </ul>
  
    </div>

  </div>
</main>

<footer>
  <%- include('../partials/footer'); %>
</footer>

</body>
</html>
